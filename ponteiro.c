#include <stdio.h>
#include <stdlib.h>
/*int main()
{
    int *p;
    p = (int *)malloc(sizeof(int));
    if (p == NULL)
    {
        printf("Erro ao alocar memória.\n");
        return 1;
    }
    *p = 42;
    printf("Valor apontado por p: %d\n", *p);
    free(p);
    return 0;
} */
struct Pessoa{
    float altura;
    int idade;
    float peso;
};
int main(){
    return 0;
}